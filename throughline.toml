[project]
name = "GOV.UK Design System components (throughline source)"
format_version = 3

# A throughline *source*: a standalone graph a consuming project composes via
# throughline-compose, importing it under a namespace (e.g. `govuk`) and referencing
# clauses as `govuk:SR-0001`. The UIDs here are this source's own and immutable; the
# component's native anchor (e.g. `date-input` or `date-input#accessibility`) lives in
# `attrs.source_ref`, never in the UID.

[grounding]
root_types = ["intent"]
delivery_roots = ["intent"]
ground_link_types = ["derives_from", "implements"]
ai_origins = ["ai", "hybrid"]

# The "why" spine has as many roots as the Design System has genuine purposes — not one
# forced umbrella. There are three co-equal **root** intents, the outcomes it claims:
#   INT-0002  Accessibility        (works for everyone; WCAG 2.2 AA + the 2018 regs)
#   INT-0003  Consistent use       (recognisable, reused correctly across services)
#   INT-0004  Proven by research   (validated by user testing)
# Each component is a user_requirement grouping the standard; it `derives_from` the
# specific outcome roots its clauses serve. Each usage rule / accessibility criterion /
# do-and-don't is a system_requirement that `implements` its component UR and `serves`
# the outcome root matching its `kind` (accessibility → INT-0002, usage → INT-0003,
# research → INT-0004), so a clause's *why* is a first-class, traversable edge rather
# than a flat tag. Each item also carries a `rationale` — its own reason for being.
[types.user_requirement]
attrs.source_ref = { required = true }

[types.system_requirement]
attrs.source_ref = { required = true }
attrs.kind = { required = true, values = ["usage", "accessibility", "research"] }

[links]
types = ["derives_from", "implements", "serves", "relates"]

[link_rules]
derives_from = { from = ["user_requirement"], to = ["intent"] }
implements   = { from = ["system_requirement"], to = ["user_requirement"] }
serves       = { from = ["system_requirement"], to = ["intent"] }

[status]
values = ["proposed", "draft", "deferred", "approved", "ratified", "implemented",
          "verified", "suspect", "rejected", "deleted"]

[transitions]
proposed    = ["draft", "approved", "ratified", "deferred", "rejected", "deleted"]
draft       = ["approved", "deferred", "rejected", "deleted"]
deferred    = ["draft", "approved", "rejected", "deleted"]
approved    = ["ratified", "implemented", "deferred", "suspect", "rejected", "deleted"]
ratified    = ["implemented", "suspect", "rejected", "deleted"]
implemented = ["verified", "suspect", "rejected", "deleted"]
verified    = ["implemented", "suspect", "deleted"]
suspect     = ["approved", "ratified", "implemented", "verified", "rejected", "deleted"]
rejected    = ["draft", "deleted"]

# `tl docs` regenerates docs/*.md from the graph; `tl docs --check` gates it.
# This activates the `unpublished` rule: every live normative item must be
# referenced by a published document or `tl check --strict` fails.
[docs]
paths = ["README.md", "docs/*.md"]

# Semantic status roles (SR-0131): which declared status
# plays each role the tool's operations act on. Backfilled by
# `tl migrate`; edit to match your own status vocabulary.
[status.roles]
initial = "draft"
proposed = "proposed"
ratified = "ratified"
invalidated = "rejected"
suspect = "suspect"
tombstone = "deleted"
